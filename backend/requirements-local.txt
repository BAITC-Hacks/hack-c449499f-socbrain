# Self-hosted распознавание и диаризация (STT_PROVIDER=local) — ставится в образ worker.
# faster-whisper >= 1.1: нужен multilingual (язык по фрагментам) для шала-казахской речи.
faster-whisper>=1.1
sherpa-onnx>=1.10
numpy>=1.26
