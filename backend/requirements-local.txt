# Ставится отдельно, когда переключаемся на self-hosted распознавание
# (STT_PROVIDER=local) — не устанавливаем сразу, faster-whisper тянет
# тяжёлые зависимости (torch и веса модели, несколько ГБ).
faster-whisper==1.0.3
