# Ставится отдельно, когда переключаемся на self-hosted распознавание
# (STT_PROVIDER=local). Использует CTranslate2; веса модели готовятся отдельно.
faster-whisper==1.0.3
# faster-whisper 1.0.3 импортирует requests, но не объявляет его в зависимостях.
requests>=2.32.4,<3
